<views:MvxWpfView
    xmlns:views ="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
    xmlns:mvx="clr-namespace:MvvmCross.Platforms.Wpf.Binding;assembly=MvvmCross.Platforms.Wpf"
    x:Class="MvxElebris.Wpf.StatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MvxElebris.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Stat Name" />
            <TextBox Text="{Binding StatName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 15"/>
            <TextBlock Text="Stat Name" />
            <TextBox Text="{Binding GoverningAttribute, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 15"/>
            <TextBlock Text="Stat Name" />
            <TextBox Text="{Binding BaseValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Margin="0 0 0 15"/>
            <Button mvx:Bi.nd="Command AddStatCommand" Margin="0 0 0 15" >Add Stat</Button>

            <DataGrid ItemsSource="{Binding Stats}" AutoGenerateColumns="True"></DataGrid>
        </StackPanel>  
    </Grid>
</views:MvxWpfView>
