public class GenerateUnit
{
    //provide a few ways to generate a character vie overload

    //default generates random classes and then uses those as biases

    //overload 1 takes a list of classes and then passes in attribute biases, only goes up to 3 classes. if less than 3 roll random to fill


    // step 2 is setting stats based on attributes

    //step 3 is traits and background

    //step 4 is randomly generated (valid) gear

    //return completed unit
}