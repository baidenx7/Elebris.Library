namespace Elebris.Library.Units.Utils.CalculationObjects
{
    public class AddFormula : IFormulaObject
    {
    //    add: (a, b, c) =>
    //c != 0 ? (((b + c) / c + 0.5 * (a - 1)) / (b / c)) * a* b : b* a,
        public float Calculate()
        {
            throw new NotImplementedException();
        }
    }
}
