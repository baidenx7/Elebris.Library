using System;

namespace Elebris.Library.Units.Utils.CalculationObjects
{
    public class DecayFormula : IFormulaObject
    {
        // decay: (a, b, c) => (a * b) / (a + c),
        public float Calculate()
        {
            throw new NotImplementedException();
        }
    }
}
