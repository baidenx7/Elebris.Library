using System;

namespace Elebris.Library.Units.Utils.CalculationObjects
{
    public class ReduceLowerFormula : IFormulaObject
    {
        //reducelower: (a, b, c) => b - c * (a + 1),
        public float Calculate()
        {
            throw new NotImplementedException();
        }
    }
}
