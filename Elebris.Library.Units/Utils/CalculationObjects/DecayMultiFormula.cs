using System;

namespace Elebris.Library.Units.Utils.CalculationObjects
{
    public class DecayMultiFormula : IFormulaObject
    {
        //decaymulti: (a, b, c) => 1 + (a* b) / (a + c),
        public float Calculate()
        {
            throw new NotImplementedException();
        }
    }
}
