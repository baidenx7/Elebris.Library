using System;

namespace Elebris.Library.Units.Utils.CalculationObjects
{
    public class IntervalAddFormula : IFormulaObject
    {
        //  intervaladd: (a, b, c) => b + Math.floor(a / c),
        public float Calculate()
        {
            throw new NotImplementedException();
        }
    }
}
