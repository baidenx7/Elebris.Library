using System;

namespace Elebris.Library.Units.Utils.CalculationObjects
{
    public class LogNumFormula : IFormulaObject
    {
        //log: (num) => Math.log(Math.max(num, 1)) / 2.303,
        public float Calculate()
        {
            throw new NotImplementedException();
        }
    }
}
