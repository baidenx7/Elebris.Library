<Window
    x:Class="CaliburnWPFApp.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        
        mc:Ignorable="d" Background="BurlyWood"

        Title="ShellView" Height="600" Width="1000">
          
    <DockPanel>
        <Menu DockPanel.Dock="Top" FontSize="18">
            <MenuItem Header="_File">
                <MenuItem x:Name="ExitApplication" Header="E_xit" />
            </MenuItem>
            <MenuItem Header="_Users">
                <MenuItem x:Name="UserManagement" Header="User_Management" />
            </MenuItem>
            <MenuItem Header="_Account" Visibility="{Binding IsLoggedIn, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}">
                <MenuItem x:Name="LogOut" Header="_Log Out" />
            </MenuItem>

        </Menu>
        <Grid>
            <!--View: Display Panel-->
            <StackPanel  Grid.Column="1" Grid.Row="0" Grid.RowSpan="3" Grid.ColumnSpan="3" Margin="5,-10,20,20">
                <!--Caliburn Micro used the name ActiveItem to bind ActivateItemAsync() to a particular view(model) -->
                <ContentControl x:Name="ActiveItem" Margin="20 5 20 20" />
            </StackPanel>
        </Grid>

    </DockPanel>
</Window>
